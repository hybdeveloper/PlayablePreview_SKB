{
  "name": "playable-preview",
  "version": "1.0.0",
  "private": true,
  "description": "Drive-style browser + AppLovin-style previewer for HTML playable ads, deployable to GitHub/GitLab Pages.",
  "scripts": {
    "dev": "node scripts/serve.js",
    "build": "node scripts/build.js --out dist"
  },
  "engines": {
    "node": ">=18"
  }
}
